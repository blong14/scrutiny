FROM golang:1.16-bullseye AS go-build

RUN apt-get update && \
    apt-get install -y libbrotli-dev

WORKDIR /go/src

COPY go.mod /go/src
COPY go.sum /go/src
RUN go mod download

COPY . /go/src
RUN go build -o /go/bin/ scrutiny